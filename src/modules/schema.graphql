type Query {
    getMyProfile: User!
    getAll: [User]
}

type Mutation {
    createTrainee(user: CreateUserInput): User!
    updateTrainee(id: ID!, role: String, name: String): User!
    deleteTrainee(id:ID!): ID!
}
